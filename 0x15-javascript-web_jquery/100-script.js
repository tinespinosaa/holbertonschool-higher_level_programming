// The script updates the text color of the header element to red
// using document.querySelector

(() => {
    document.addEventListener('DOMContentLoaded', () => {
      document.querySelector('header').style.color = '#FF0000';
    }, false);
  })();
